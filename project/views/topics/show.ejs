<%- include('../partials/header', { title: topic.title }) %>

<div class="max-w-4xl mx-auto px-6 py-20">
    <h1 class="font-serif text-5xl mb-8 italic text-brandAccent"><%= topic.title %>/</h1>
    
    <div class="prose prose-invert lg:prose-xl font-mono text-gray-300">
        <p><%= topic.description %></p>
    </div>

    <div class="mt-12 pt-8 border-t border-brandBorder">
        <a href="/stats" class="text-xs font-mono text-brandAccent hover:underline tracking-widest">
            ← BACK TO ANALYTICS
        </a>
    </div>
</div>

<%- include('../partials/footer') %>